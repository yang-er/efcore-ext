language: csharp
mono: none
dotnet: 5.0.101

script:
  - dotnet restore
  - dotnet build

deploy:
  skip_cleanup: true
  provider: script
  script: ./deploy.sh $NUGET_KEY
  on:
    branch: master
    tags: true
